name: Wake Up Space
on:
  schedule:
    - cron: '0 */12 * * *' # Runs every 12 hours
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Visit Space
        run: |
          curl -s -L https://ric35-midbot2.hf.space/ > /dev/null
